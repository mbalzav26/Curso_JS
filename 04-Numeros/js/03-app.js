// Funcion Math
let resultado;

resultado = Math.PI;

// Redondeo
resultado = Math.round(2.5);
resultado = Math.round(2.4);

// Redondeo hacia arriba
resultado = Math.ceil(2.1);

// Resoundeo hacia abajo
resultado = Math.floor(2.9);

// potencia
resultado = Math.pow(8,3);

// Raiz cuadrada
resultado = Math.sqrt(64);

// maximo numero
resultado = Math.max(2,3,8,-1,10);

// menor numero
resultado = Math.min(2,3,8,-1,10);

// numero alatorio
resultado = Math.random();

// valor absoluto
resultado = Math.abs(-3);

// numero aleatorio entre un rango
resultado = Math.floor(Math.random() * 20);
console.log(resultado);