// Orden de las operaciones

// El orden es el mismo de toda la vida primero la * y / despues + y -

let resultado;

resultado = 20 + 10 * 2;

// Para que tome primero la + o - y luego la * o /

resultado = (20 + 10) * 2;

// Para que tome primero la * o / y luego la + o -
resultado = 20 + (10 * 2);

// Para sumarles o quitarles un porcentaje a un numero

resultado = (20 + 10 + 30 + 40) *.2;

resultado = (20 + 10 + 30 + 40) *1.16;
console.log(resultado);