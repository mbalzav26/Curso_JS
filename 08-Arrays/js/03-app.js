// recorrer un arreglo en javascript

const meses = ["enero", "febrero", "marzo", "abril", "mayo", "junio"];

// para recorrer un arreglo primero debemos conocer su tamaño

console.log(meses.length);

// para recorrer un arreglo usamos un for

for(i = 0; i < meses.length; i++){
    console.log(meses[i]);
}

/* usando el nombre del mas el indice
obtenemos el valor del indice  ejemplo meses[i] */