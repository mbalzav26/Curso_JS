// Arrays

// Son variabbles que contienen una colección de datos

// Forma mas común de crear un array
const numeros = [1,2,3,4];
console.log(numeros);

// forma menos comun de crear un array
const datos = new Array("hola","monitor", "etc");
console.log(datos);

// Un arreglo puede contener cualquier tipo de dato
const variado = [10, "Monitor", true, null, {nombre: "mateo", sexo: "masculino"}, [1,2,3,4]];
console.log(variado);