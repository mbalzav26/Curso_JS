const producto = 'Pizza';

// para pasarlo a mayúsculas usamos .toUpperCase()

console.log(producto.toUpperCase());

// para pasarlo a minúsculas usamos .toLowerCase()

console.log(producto.toLowerCase());

// Para pasar un numero a string usamos .toString()

const numero = 12345;
console.log(numero);
console.log(numero.toString());