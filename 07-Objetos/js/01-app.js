// Objetos

const nombre = "Monitor"
const precio = 100
const disponible = true;

// Los objetos nos permiten crear todo en una sola variable

// Tienen la siguiente sintaxis:

// Esta forma se conoce como object literal
const objeto = {
    nombre: "Monitor",
    precio: 100,
    disponible: true
}

console.log(objeto);